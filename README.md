# US Buildings Sector Segmentation

A static, interactive HTML dashboard that projects US buildings sector impact segments.

## How to Build Locally
1. Install dependencies: `pip install -r requirements.txt`
2. Run the generator: `python generate_dashboard.py`
3. Open `docs/index.html` in any web browser to view the dashboard locally.